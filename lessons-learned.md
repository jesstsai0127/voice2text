## 2026-08-03 — daily_run.py 排程拆分重寫，先寫完實作才補測試（test-after）

**情境**：#22 backfill 機制實作到一半，使用者陸續補充需求（一天可能不只一集、backfill 要獨立排程、
個人音檔要更頻繁檢查），範圍從「加一個 find_all_episodes」擴大成「daily_run.py 整個拆成三個進入點
+ lock 機制」。在專注於把新架構的邏輯想清楚、一口氣寫完 daily_run.py 的過程中，沒有先為
`run_daily_podcast_check`/`run_podcast_backfill` 的近期窗口過濾、批次上限、`is_filename_recorded`
預檢跳過等新邏輯寫失敗測試，是實作完才回頭補的（test_daily_run_scheduling.py）。

**錯誤做法**：範圍在對話中途擴大時，順著設計思路一路寫到底，沒有在每個新函式動手前停下來先寫紅燈測試。

**正確做法**：即使是同一個任務內的範圍擴張，只要是新的程式邏輯（不是套用既有已測試過的功能），
就要照 red→green 的順序，一個函式寫完就先補測試確認情境，不要等到整個檔案的多個函式都寫完才一次補測試。

**預防方式**：範圍擴大時，先停下來把新增的函式清單列出來（哪怕只是心裡默念），
逐一走「寫測試→確認紅燈→寫最小實作→確認綠燈」，不要因為設計思路連貫就跳過中間的紅燈確認步驟。
